name: multicamp_unsplash
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  mobx: ^1.2.1+2
  flutter_mobx: ^1.1.0+2
  http: ^0.12.2
  mobx_codegen: ^1.1.0+1
  flutter_staggered_grid_view: ^0.3.2
  url_launcher: ^5.7.10
  cached_network_image: ^2.4.1
  photo_view: ^0.10.3
  permission_handler: ^5.0.1+1
  toast: ^0.1.5
  media_store: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.1

flutter:
  uses-material-design: true
